<div class="flex items-center gap-3">
    <img src="{{ asset('images/logo.jpeg') }}" alt="" class="size-9 shrink-0 rounded">

    <div class="min-w-0 leading-tight">
        <p class="truncate text-sm font-semibold">{{ config('app.name') }}</p>
        <p class="truncate text-xs opacity-60">Sistem Front Office Elektronik</p>
    </div>
</div>
